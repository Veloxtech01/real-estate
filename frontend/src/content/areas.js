/**
 * "Areas we cover" index copy (§4.1). Local to the repo, same content-source decision
 * as about.js/contact.js/listYourProperty.js.
 */
const areasContent = {
  eyebrow: "Where we operate",
  title: "Areas we cover",
  intro:
    "Browse listings by neighbourhood. Each area page covers what it's like to live there and the homes currently available.",
};

export default areasContent;
